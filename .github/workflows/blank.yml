name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          apt update -y
          apt install python3 python3-pip -y
          pip install checkov
          checkov -v
